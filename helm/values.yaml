# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: vladbatrak
  appname: telegram-bot
  tag: "v1.0.3-df119c2"
  arch: amd64

secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"